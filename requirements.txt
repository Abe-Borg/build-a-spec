# Build-a-Spec backend dependencies (Python 3.11+)
anthropic>=0.117,<1
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.10
platformdirs>=4.3
pywebview>=6.0
python-docx>=1.1
pypdf>=4
python-multipart>=0.0.9

# Optional (recommended): OS credential-manager storage for the API key
keyring>=25

# Development / tests
pytest>=8
httpx>=0.28
