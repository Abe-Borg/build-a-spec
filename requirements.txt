# Build-a-Spec backend dependencies (Python 3.11+)
anthropic>=1.0,<2
fastapi>=0.115
uvicorn>=0.32
pydantic>=2.10
platformdirs>=4.3
pywebview>=6.0
python-docx>=1.1
lxml>=5
pypdf>=4
python-multipart>=0.0.9

# Optional (recommended): OS credential-manager storage for the API key
keyring>=25

# Development / tests
pytest>=8
httpx>=0.28
# The lint gate (ruff.toml). Pinned exactly: a ruff minor can add rules to
# the selected families and turn CI red on an unrelated PR.
ruff==0.15.8
